[DEFAULT]
logcfg = logging.ini

feed = devio
runner = dask
registry = homedir

[FEED:devio]
provider = devio

[RUNNER:dask]
provider = dask
scheduler = multiprocessing

[RUNNER:graphviz]
provider = graphviz
format = png

[REGISTRY:virtual]
provider = virtual

[REGISTRY:homedir]
provider = filesystem
#path = ~/.forml/registry
