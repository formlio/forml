[DEFAULT]
log_cfgfile = logging.ini

engine = devio
runner = dask
registry = homedir


[ENGINE:devio]
provider = devio

[RUNNER:dask]
provider = dask
scheduler = multiprocessing

[RUNNER:graphviz]
provider = graphviz
format = png

[REGISTRY:homedir]
provider = filesystem
#path = ~/.forml/registry
